---
- name: Deploy global Git configuration
  template:
    src: gitconfig.j2
    dest: "{{ ansible_facts.user_dir }}/.gitconfig"
    mode: "0644"

- name: Deploy global gitignore
  copy:
    src: gitignore_global
    dest: "{{ ansible_facts.user_dir}}/.gitignore_global"
    mode: "0644"
